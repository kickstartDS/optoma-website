diff --git a/lib/index.js b/lib/index.js
index d91a9e5c64f7a2937df9fd3ff9c47bd5982335b6..6c85e627e7e0058436e68c7579757a7199a294ad 100644
--- a/lib/index.js
+++ b/lib/index.js
@@ -472,6 +472,13 @@ function processBasic({ name, description, subSchema, rootSchema }) {
         field.field_type = 'storyblok-icon-sprite-picker-field-plugin';
         field.options = [{ name: 'allowedIcons', value: JSON.stringify(subSchema.enum), _uid: uuidv4() }];
     }
+    if (subSchema.type === 'string' && subSchema.format && subSchema.format === 'theme') {
+        field.type = 'custom';
+        field.field_type = 'storyblok-theme-select-field-plugin';
+    }
+    if (subSchema.type === 'string' && subSchema.format && subSchema.format === 'textarea') {
+        field.type = 'textarea';
+    }
     if (subSchema.type === 'string' && subSchema.format && subSchema.format === 'image') {
         field.type = 'asset';
         field.filetypes = ['images'];
